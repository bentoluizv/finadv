# Alembic config. URL is set in env.py from settings (DATABASE_URL env or .env).
# Run from project root: uv run alembic upgrade head

[alembic]
script_location = alembic
prepend_sys_path = .
version_path_separator = os
